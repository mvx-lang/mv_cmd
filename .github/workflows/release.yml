# Release cmd on a version tag: publish the source package and prebuilt binary
# packages (mvx + UniData) as GitHub release assets.  The package registry
# indexes the assets via its release webhook (it hosts nothing).  Each job
# publishes to the same release; asset names mirror the registry's artifact key
# (<name>-<ver>-<suffix>, base = package name '/'->'_') so the webhook maps them:
#   source                        -> cmd-<ver>-source.tar.gz
#   mvx binary  (linux/x86_64,le) -> cmd-<ver>-mvx-linux-x86_64-le.tar.gz
#   udt binary  (linux/x86_64,le) -> cmd-<ver>-udt-linux-x86_64-le.tar.gz
#
# cmd is pure BASIC.  The source package is the BP/ programs (compiled on
# install); the mvx binary is the compiled subroutine library (LIB/libcmd.so)
# an account links with no recompile; the udt binary is the prebuilt UniData
# objects (BP/_CMD.*) that MVPKG-udt catalogs with no compiler.
# Copyright (C) 2026 Gordon Heydon.  GPL-2.0-only.
#
# Tag the bare version to release, e.g.:  git tag 0.2 && git push origin 0.2
name: release

on:
  push:
    tags:
      - "[0-9]+.[0-9]+*"
      # ONE rolling dev tag, moved to the HEAD of develop by the nightly when
      # there is something new.  It builds through THIS workflow rather than
      # a second copy of it, so a dev build is the same artifacts by the same
      # steps as a release -- the only way a dev build is worth having.
      - "dev"

  # A TAG PUSHED BY A WORKFLOW DOES NOT TRIGGER ONE.  GitHub will not let the
  # default GITHUB_TOKEN start another run -- it is how recursion is prevented
  # -- so the nightly's tag lands and nothing happens.  It dispatches this
  # workflow on the tag instead, which is the same build from the same ref.
  workflow_dispatch:

permissions:
  contents: write

jobs:
  # ---- source package: the BP/ programs, portable across MVX and UniData ----
  source:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: mvx-lang/mvx/.github/actions/publish-source@main
        with:
          package: mvx-lang/cmd
          artifact: cmd
          title: cmd
          body: "The cmd command framework (CMD.INIT / CMD.ADD / CMD.RUN). Source + prebuilt mvx binary; indexed by the package registry."

  # ---- mvx binary: compile the subroutine library against a published mvx ----
  mvx-binary:
    # The setup-mvx tarball is built on ubuntu-latest; run on a matching glibc.
    runs-on: ubuntu-24.04
    steps:
      - name: Check out cmd
        uses: actions/checkout@v5

      - name: Check out the mvx package tooling (scripts/mkpkg.sh)
        uses: actions/checkout@v5
        with:
          repository: mvx-lang/mvx
          path: .mvx-src
          sparse-checkout: scripts
          sparse-checkout-cone-mode: false

      - name: Set up the mvx toolchain (latest published build)
        uses: mvx-lang/mvx/.github/actions/setup-mvx@main
        with:
          version: latest

      - name: Build the mvx binary package
        id: pkg
        shell: bash
        run: |
          set -euo pipefail
          VER="${GITHUB_REF_NAME}"
          ARCH="$(uname -m)"                        # ubuntu-24.04 -> x86_64
          OS="$(uname -s | tr 'A-Z' 'a-z')"; case "$OS" in *linux*) OS=linux ;; esac
          case "$(printf '\1\2' | od -An -tx2 | tr -d ' \n')" in
            0201*) ENDIAN=le ;; 0102*) ENDIAN=be ;; *) ENDIAN=le ;;
          esac
          BASE="cmd-$VER-mvx-$OS-$ARCH-$ENDIAN"

          # Compile BP/ into LIB/libcmd.so against the installed mvx-basic.
          MVXBASIC="$(command -v mvx-basic)" sh .mvx-src/scripts/mkpkg.sh .
          test -f LIB/libcmd.so

          # Assemble a binary-only package: the prebuilt LIB the runtime links,
          # plus the manifest/verb records — no BP source (installs identically
          # to a source package via LINK-PKG, with nothing to recompile).
          mkdir -p "dist/$BASE"
          cp -a LIB PKG mvpkg.json LICENSE README.md "dist/$BASE/"
          for d in VOC VOC.DICT LIB.DICT; do [ -e "$d" ] && cp -a "$d" "dist/$BASE/" || true; done
          rm -rf "dist/$BASE"/LIB/*.dSYM "dist/$BASE"/LIB/*.debug   # ship the library, not debug bundles
          tar czf "$BASE.tar.gz" -C "dist/$BASE" .   # contents at the tar root (no wrapping dir)
          sha256sum "$BASE.tar.gz" > "$BASE.tar.gz.sha256"
          echo "tarball=$BASE.tar.gz" >> "$GITHUB_OUTPUT"
          echo "checksum=$BASE.tar.gz.sha256" >> "$GITHUB_OUTPUT"

      - name: Publish the mvx binary asset
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ github.ref_name }}
          files: |
            ${{ steps.pkg.outputs.tarball }}
            ${{ steps.pkg.outputs.checksum }}
          fail_on_unmatched_files: true

  # ---- udt binary: prebuilt UniData objects, built in the udt-builder --------
  # Runs on the self-hosted udt runner; compiles BP/CMD.* to _objects in the
  # udt-builder container and ships them as an objects-only package (MVPKG-udt
  # catalogs the objects with no compiler).  Independent of the jobs above — if
  # the runner is offline this job is skipped/failed without affecting the
  # source and mvx assets, which the registry already indexes on their own.
  udt-binary:
    runs-on: [self-hosted, linux, udt]
    steps:
      - uses: actions/checkout@v5
      - uses: mvx-lang/mv-package-registry/.github/actions/udt-build@main
        with:
          package: mvx-lang/cmd
          artifact: cmd

  # ---- what kind of release is this? ----------------------------------------
  # LAST, because it is correcting the publish steps rather than joining them.
  # Both of them create the release as a side effect of uploading to it, and
  # neither is told what the release is: publish-source marks a pre-release with
  # `contains(ref_name, '-')`, which is FALSE for `dev`; udt-build hands
  # softprops only a tag_name, and its defaults are `prerelease: false` and
  # latest.  So whichever finishes last decides, and left alone a nightly dev
  # build would present itself as the newest stable one -- to the registry, and
  # to anyone installing this package by name.
  #
  # Both directions are set explicitly, for the same reason: leaving the stable
  # case alone means it keeps whatever that race happened to leave behind.
  # ---- uv and jbase: the SOURCE package, staged on a hosted runner -----------
  # Source, where the udt artifact above ships prebuilt objects -- and that is the
  # platforms differing, not a shortcut.  UniData's object for a program is a FILE
  # beside it, so build-udt.sh can lift the _CMD.* out and MVPKG-udt catalogs them
  # with no compiler.  jBASE's is not: cataloging there aggregates programs into a
  # per-account shared library ($HOME/lib/lib0.so), so there is no per-program
  # object to lift and nothing portable to stage.
  #
  # uv and jbase take the source and MVPKG catalogs it on the target; both ship a
  # BASIC compiler as standard, so the only cost is a compile at install.  Hence
  # `any-any-le` and a hosted runner -- nothing here is compiled, so there is
  # nothing to lock to an os or arch, and no licensed container is needed.
  #
  # AFTER source and ONE AT A TIME: each upload creates the release if it is not
  # there yet, so jobs starting together race and all but one lose with
  # `already_exists` on tag_name (curl-cmd#7).
  account:
    needs: [source]
    strategy:
      max-parallel: 1
      matrix:
        system: [uv, jbase]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Stage the ${{ matrix.system }} account
        id: pkg
        shell: bash
        run: |
          set -euo pipefail
          VER="${GITHUB_REF_NAME}"
          BASE="cmd-$VER-${{ matrix.system }}-any-any-le"
          rm -rf dist && mkdir -p dist
          sh build-pkg.sh dist
          tar czf "$BASE.tar.gz" -C dist cmd
          sha256sum "$BASE.tar.gz" > "$BASE.tar.gz.sha256"
          echo "tarball=$BASE.tar.gz" >> "$GITHUB_OUTPUT"
          echo "checksum=$BASE.tar.gz.sha256" >> "$GITHUB_OUTPUT"
      - name: Publish the ${{ matrix.system }} account asset
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ github.ref_name }}
          files: |
            ${{ steps.pkg.outputs.tarball }}
            ${{ steps.pkg.outputs.checksum }}
          fail_on_unmatched_files: true

  mark:
    needs: [source, mvx-binary, udt-binary, account]
    runs-on: ubuntu-latest
    steps:
      - name: Say what this release is
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
          TAG: ${{ github.ref_name }}
        run: |
          set -euo pipefail
          # STABLE IS THE ONE THAT IS NOT A PRE-RELEASE.  dev, beta and rc all
          # are; a stable tag is anything with no suffix -- 2.1.0, not
          # 2.1.0-rc1 and not dev.
          case "$TAG" in
            dev|*-*) gh release edit "$TAG" --prerelease --latest=false
                     echo "$TAG is a pre-release, and not latest." ;;
            *)       gh release edit "$TAG" --prerelease=false --latest
                     echo "$TAG is a stable release, and is latest." ;;
          esac
