# Derived build artifacts (build-native.sh / mkpkg output)
CATALOG/
CATALOG.DICT/
LIB/
LIB.DICT/
bin/
mvxdata.lmdb/
BP.O/
BP.O.DICT/
*.dSYM/
*.o
*.dylib
*.so

# PLATFORM.H is a per-platform build artifact (mkpkg.sh / MVPKG init write it),
# generated both at the package root and beside the BP sources.
**/PLATFORM.H

# The VOC's own dictionary is the platform's, not the account's - every
# account is born with it, and a clone gets its own (mv_git#95).
VOC.DICT/
